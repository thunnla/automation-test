/**
 * UI Test Spec — Login
 *
 * Loads data/ui/login.json and dynamically generates all test cases
 * through the Universal Test Engine UI runner.
 */

import { runUiSuite } from '../../src/core';

runUiSuite('ui/login.json');
