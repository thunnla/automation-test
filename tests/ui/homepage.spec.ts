/**
 * UI Test Spec — Homepage
 *
 * Loads data/ui/homepage.json and dynamically generates all test cases
 * through the Universal Test Engine UI runner.
 */

import { runUiSuite } from '../../src/core';

runUiSuite('ui/homepage.json');
